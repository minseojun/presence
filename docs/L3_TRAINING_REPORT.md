# PRESENCE L3 training report

Generated 2026-08-19T06:59:20.182Z from 0 session(s) in `server/data/sessions`.

**Insufficient data (0 sessions).** Existing default weights in `server/data/model/weights.json` were left unchanged. Collect at least ~10 normal + ~10 remote sessions via the 수집 tab (or `node scripts/train_l3_model.js --synthetic` to sanity-check the pipeline itself) before training for real.
