# L1 ML classifier training report (auto-generated by `node scripts/train_l1_model.js`)

Generated 2026-08-27T14:28:00.553Z.

**무엇을 학습했나**: `server/src/l1.js`가 이미 계산하는 어휘적 특징(브랜드 유사도,
의심 TLD, 엔트로피 등 — 손으로 짠 휴리스틱과 동일한 입력)을 그대로 특징 벡터로 써서
실제 라벨링된 도메인으로 로지스틱 회귀를 학습시켰다. 딥러닝이 아니라 13차원
선형 분류기지만, 가중치 자체는 더 이상 손으로 찍은 값이 아니라 진짜 경사하강법
학습 결과다.

- 데이터: 피싱 10,000건 + 정상 10,000건 (클래스 균형을 위해 다운샘플링, 원본 391,753건/10,000건 중)
- 학습/평가 분할: 80% 학습(16000건) / 20% 홀드아웃 평가(4000건, 학습에 전혀 안 씀)
- 특징: isIp, isPunycode, isShortener, isSuspiciousTld, brandLookalike, brandSubstring, brandPlusGenericWord, subdomainDepth, hyphenCount, digitRatio, hostEntropy, closestDistCapped, hostLenNorm

## 홀드아웃 평가 (학습에 쓰지 않은 20%)

| 임계값(ML score≥) | recall | FPR | precision |
|---|---|---|---|
| 20 | 79.3% | 8.79% | 90.3% |
| 30 | 76.5% | 5.64% | 93.3% |
| 45 | 71.6% | 2.03% | 97.3% |
| 60 | 59.1% | 0.41% | 99.3% |
| 75 | 46.9% | 0.30% | 99.4% |

실제 서버는 이 ML 점수를 단독으로 쓰지 않고, 기존 휴리스틱 점수와 `max()`로
앙상블한다(`server/src/l1.js`) — 둘 중 하나라도 위험하다고 하면 위험으로 판정.
그래서 ML 모델이 학습 데이터의 편향을 갖고 있어도 기존에 휴리스틱이 잡던 걸
놓치게 만들 수는 없고, 휴리스틱이 놓치는 패턴을 추가로 잡는 것만 가능하다.
결합된 시스템의 실제 재현율/오탐률은 `npm run eval:l1`(`docs/L1_EVAL_REPORT.md`)
참고 — 이 리포트는 ML 모델 단독 성능만 보여준다.
